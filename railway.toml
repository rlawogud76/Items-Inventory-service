[build]
builder = "nixpacks"
buildCommand = "npm install && cd web/client && npm run build"

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
