# 재고 관리 시스템

마인크래프트 마을 재고를 실시간으로 관리하는 웹 대시보드입니다.

## ✨ 주요 기능

- 📦 **실시간 재고 관리**: 재고 현황을 실시간으로 확인하고 관리
- 🔨 **제작 시스템**: 제작품 관리 및 레시피 시스템
- 🏷️ **태그 관리**: 아이템 그룹화 및 태그 색상 커스터마이징
- 📊 **대시보드**: 전체 진행률, 활동 현황, 주의 필요 항목 한눈에 확인
- 👥 **작업자 관리**: 수집/제작 중인 사람 표시
- 📊 **프로그레스 바**: 진행률을 시각적으로 표시
- 🏆 **기여도 시스템**: 사용자별 기여도 추적 및 순위
- ⚡ **배점 시스템**: 아이템별 포인트 설정
- 🔐 **권한 관리**: 관리자/멤버 권한 설정
- 🔄 **실시간 동기화**: WebSocket 기반 실시간 업데이트
- 📋 **수정 내역 추적**: 모든 변경사항 자동 기록
- 💾 **MongoDB**: 클라우드 데이터베이스 저장

## 🚀 설치 방법

### 1. Node.js 설치
Node.js가 설치되어 있지 않다면 [nodejs.org](https://nodejs.org/)에서 다운로드하세요.

### 2. 패키지 설치
```bash
npm install
```

### 3. 환경 변수 설정

`.env` 파일 생성:

```env
# MongoDB 연결
MONGO_URI=mongodb+srv://...

# Discord OAuth (웹 로그인용)
DISCORD_CLIENT_ID=your_client_id
DISCORD_CLIENT_SECRET=your_client_secret
DISCORD_REDIRECT_URI=http://localhost:3001/auth/callback
DISCORD_GUILD_ID=your_guild_id

# 서버 설정
PORT=3001
JWT_SECRET=your_jwt_secret

# 클라이언트 URL
CLIENT_URL=http://localhost:5173
```

### 4. 실행

**개발 모드 (서버 + 클라이언트 동시 실행):**
```bash
npm run dev
```

**개별 실행:**
```bash
# API 서버만
npm run server

# 클라이언트만
npm run client
```

**프로덕션:**
```bash
npm run build
npm run start:prod
```

## 📁 프로젝트 구조

```
├── shared/           # 공용 모듈
│   ├── database.js   # MongoDB 연결 및 쿼리
│   ├── models/       # Mongoose 모델
│   └── services/     # 공용 서비스
├── web/
│   ├── client/       # React 프론트엔드
│   │   ├── src/
│   │   │   ├── components/  # UI 컴포넌트
│   │   │   ├── pages/       # 페이지 컴포넌트
│   │   │   ├── contexts/    # React Context
│   │   │   └── services/    # API 서비스
│   │   └── package.json
│   └── server/       # Express 백엔드
│       ├── routes/   # API 라우트
│       ├── middleware/
│       └── package.json
└── package.json      # 워크스페이스 설정
```

## 🔑 Discord OAuth 설정

1. [Discord Developer Portal](https://discord.com/developers/applications)에서 앱 생성
2. OAuth2 > General에서 Redirects에 `http://localhost:3001/auth/callback` 추가
3. Client ID와 Client Secret을 `.env`에 설정

## 📄 라이선스

MIT
